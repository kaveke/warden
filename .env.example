# ==========================================
# Warden SOAR Lab - Environment Configuration
# ==========================================
# Copy this file to .env and configure with your actual values
# Never commit .env to Git - it contains secrets

# === n8n Public Configuration ===
# The public hostname where n8n is accessible
# Examples:
#   - Cloudflare Quick Tunnel: your-subdomain-here.trycloudflare.com
#   - Cloudflare Named Tunnel: warden.yourdomain.com
#   - Local testing: localhost
N8N_HOST=localhost

# Port n8n listens on inside the container and exposes on host
N8N_PORT=5678

# Protocol for internal communication
# Use 'http' - Cloudflare handles HTTPS at the edge
N8N_PROTOCOL=http

# Full webhook URL (auto-constructed from above)
# For local testing: http://localhost:5678
# For Cloudflare: https://${N8N_HOST}
WEBHOOK_URL=http://${N8N_HOST}:${N8N_PORT}

# Enable secure cookies (set to 'true' when using HTTPS via Cloudflare)
N8N_SECURE_COOKIE=false

# === Redis Configuration ===
# REQUIRED: Generate a strong password (e.g., openssl rand -base64 32)
REDIS_PASSWORD=CHANGE_ME_TO_STRONG_PASSWORD

# === Network Configuration ===
# Your VirtualBox host-only adapter IP
# Find with: ipconfig (Windows) or ip a (Linux/Mac)
HOST_IP=192.168.56.1

# === Wazuh Configuration ===
# Your Wazuh manager address (used by n8n workflows for deep links)
WAZUH_URL=https://192.168.56.111

# === Application Settings ===
# Your timezone (affects log timestamps and scheduled workflows)
# Examples: America/New_York, Europe/London, Asia/Tokyo
TIMEZONE=Africa/Nairobi

# ==========================================
# Optional: Advanced Configuration
# ==========================================

# === Cloudflare Tunnel (if using Named Tunnel) ===
# CLOUDFLARED_TUNNEL_TOKEN=your-tunnel-token-here

# === Slack Configuration (can also configure in n8n UI) ===
# SLACK_BOT_TOKEN=xoxb-your-token-here
# SLACK_SIGNING_SECRET=your-secret-here
# SLACK_CHANNEL=#security-alerts

# === VirusTotal API ===
# VIRUSTOTAL_API_KEY=your-api-key-here

# === Worker Configuration ===
# VM_CONFIG_PATH=./vm_config.json
